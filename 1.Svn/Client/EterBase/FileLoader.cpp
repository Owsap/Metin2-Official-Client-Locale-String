//Find in void CMemoryTextFileLoader::Bind(int bufSize, const void* c_pvBuf)
		else if (c < 0)
		{
			stLine.append(c_pcBuf + (pos-1), 2);
			++pos;
		}

///Change
		/*else if (c < 0)
		{
			stLine.append(c_pcBuf + (pos-1), 2);
			++pos;
		}*/